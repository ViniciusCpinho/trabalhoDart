

import 'package:flutter/material.dart';

class AdvertisingHome extends StatelessWidget {

  @override
  Widget build(BuildContext context) {

    return Container(
      child: Column(
        children: [
          
        ],
      ),
    );
  }
}